# Ajantala website — local V1

Static, dependency-free preview of the Ajantala illustrated folktale.

## Run locally

~~~bash
python3 -m http.server 8000
~~~

Open http://localhost:8000

The PNGs in `public/assets/` are copies of the approved files in `../source-images/`. The source artwork is not modified.
